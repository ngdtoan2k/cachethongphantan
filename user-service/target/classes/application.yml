server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/user_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: 5672
    username: guest
    password: guest

# Cấu hình logging cơ bản
logging:
  level:
    root: INFO
    com.ecommerce.user: DEBUG          # log chi tiết cho code nội bộ
    org.springframework.amqp: WARN    # chỉ log cảnh báo cho RabbitMQ
    org.springframework.web: WARN
    org.hibernate.SQL: DEBUG           # log các câu SQL
    org.hibernate.type.descriptor.sql: WARN
  pattern:
    console: "%d{HH:mm:ss} [%thread] [TraceId: %X{traceId:-}, SpanId: %X{spanId:-}] %-5level %logger{36} - %msg%n"

